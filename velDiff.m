function [a,b] = velDiff(objA)
    %inputs a experiment data struct and outputs the diff relative to the 
    % nominal speed over sample time.
    
    op = objA.op; %Operating point
    n_comp=objA.rpm_comp; %Nominal comp speed
    n_motor=objA.rpm_motor; %Nominal Motor speed
    t=objA.time;    %sample time
    temp= 
    
    
    
    
    
    
    
    
    
       

end